<div class="box">
  <article class="media">
    <div class="media-content">
      <div class="content">
        <%= board.song_title %>
        <%= board.artist %>
        <%= board.song_image %>
        <%= link_to board.title, "/boards/#{board.id}" %>
      </div>
    </div>
  </article>
</div>